export const useTerrain = () => {
  const analiceTerrainDistance = () => {
    // Aqui es pot consultar a una punt d'accés per obtenir la distancia del "planeta"
    return { x: 200, y: 200 }
  }
  return { analiceTerrainDistance }
}
